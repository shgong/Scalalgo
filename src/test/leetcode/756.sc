// Pyramid Transition Matrix
def pyramidTransition(bottom: String, allowed: List[String]): Boolean = {


  def generate_status(allowed: List[String], bottom:String):Boolean = {
    val l = allowed.length
    if (l == 1)  true
    else {
      for(i<-0 until l-1){

      }


    }
  }

  generate_status(allowed, bottom)
}

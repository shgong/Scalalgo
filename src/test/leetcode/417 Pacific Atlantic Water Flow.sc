object Solution {
  def pacificAtlantic(matrix: Array[Array[Int]]): List[Array[Int]] = {
    val h = matrix.length
    val w = matrix(0).length

    // Pacific
    val pac = Array.fill(h)(Array.fill(w)(false))
    val atl = Array.fill(h)(Array.fill(w)(false))


    def dfs()= {
      // for each direction
      //    if false, compare depth
      //    set depth to true
      //        next iteration
    }

    // run at pac & atl
    // get both true points

  }
}